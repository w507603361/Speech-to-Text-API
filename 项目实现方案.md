# 录音转写与智能摘要 API：项目实现方案

本文依据同目录《后端实习生笔试项目.pdf》编写，并按最终确认范围更新：完成前六步，以本地 Docker 运行版本交付。

## 1. 目标与范围

使用 Python、FastAPI、MySQL 和真实 DeepSeek API，实现上传录音、异步转写模拟、智能摘要、任务查询、失败重试和录音删除。优先保证核心功能正确、代码简洁、便于面试讲解。

题目要求收到题目后 4 个自然日内提交，预计工作量 12～16 小时；具体日程需要结合实际截止时间确认。最终交付必须包括有完整提交历史的 GitHub 仓库、README、一键启动方式、API 调试文件和数据库建表文件。

### 必做与加分项

| 分类 | 本项目安排 |
| --- | --- |
| 全部 P0 | 上传校验、本地保存、异步状态机、Mock 转写、真实 DeepSeek 摘要、六个业务接口、统一错误、日志、建表 SQL |
| 明确不做的其他加分项 | 第 1～6 条：自动失败重试、服务重启自动恢复、SSE 流式输出、上传幂等、并发数量限制、单元测试或集成测试套件 |
| 明确不做 | 用户注册登录鉴权、前端页面、高并发优化、真实 ASR、对象存储 |

同一个文件重复上传，默认创建不同录音和任务。失败任务的重复重试保护属于 P0，必须实现，不等同于上传幂等。

仍通过 Postman 和人工联调验收 P0 功能，但不开发第 6 条加分项中的自动化测试套件。说明服务重启时任务的处理方式是 P0 要求，自动恢复执行则不实现。

## 2. 技术方案与取舍

| 组件 | 选择 | 原因 |
| --- | --- | --- |
| 语言 | Python 3.12，开发开始时核对依赖兼容性 | 环境统一，代码易读 |
| HTTP 服务 | FastAPI + Uvicorn | 请求校验、接口文档和异步接口 |
| 数据库 | MySQL 8.4，InnoDB，utf8mb4 | 持久化记录、任务与事务控制 |
| 数据访问 | SQLAlchemy 2 的异步会话 + asyncmy | 统一事务，不阻塞异步事件循环 |
| 输入和结果校验 | Pydantic | 复用 FastAPI 已使用的校验方式 |
| 大模型调用 | HTTPX AsyncClient 调用 DeepSeek Chat Completions | 只有一个供应商，不额外引入代理框架或 LLM 编排框架 |
| 后台任务 | asyncio 进程内后台任务，MySQL 记录状态 | 上传或手动重试提交后启动任务，不引入队列、固定协程池或并发上限 |
| 数据库初始化 | 一份带编号的建表 SQL | 满足题目要求，当前两张表无需额外迁移框架 |
| 启动 | Docker Compose：应用 + MySQL | 提供统一的一键启动入口 |
| 功能验收 | Postman 与人工联调 | 验证必做功能，不编写单元测试或集成测试套件 |
| 本地运行 | Docker Compose | 一键启动 FastAPI 和 MySQL，命名卷保存数据库及录音 |
| 调试交付 | Postman Collection | 可导入、保存任务 ID，并顺序演示业务流程 |

以上版本是实现基线，具体依赖补丁版本在第 1 步验证后固定。DeepSeek 模型 ID 通过配置提供，开发时核对账号实际可用且支持 JSON Output 的模型，不在业务代码里写死模型名称。

服务只运行 **1 个 Uvicorn worker、1 个应用副本**。这是部署边界，不是后台任务并发数量限制；不实现信号量、固定工作池或超额排队。不能直接增加应用进程或副本，否则启动时的中断任务标记可能影响其他实例正在执行的任务。多实例部署不属于本次范围。

### 数据流

客户端上传 → FastAPI 校验并保存本地文件 → MySQL 事务创建录音与 pending 任务 → 注册进程内后台任务并返回 202 → Mock 转写 → DeepSeek 摘要 → 保存结果与 done 状态。

客户端通过查询接口获取阶段和结果。转写或摘要失败时保存 failed 状态和可读错误，再由客户端调用重试接口。

FastAPI 使用 lifespan 管理数据库连接、共享 HTTP 客户端和后台工作协程的启动与关闭，参见 [FastAPI 官方生命周期说明](https://fastapi.tiangolo.com/advanced/events/)。

## 3. 数据结构

只使用两张业务表。每条录音固定对应一个任务，手动重试复用任务 ID，以 attempt 区分执行轮次。因此列表中的“最新任务状态”就是关联任务的当前状态，无需维护任务历史表；每次执行历史通过日志追踪。

### recordings：录音与结果

| 字段 | 含义 |
| --- | --- |
| id | UUID，录音唯一标识 |
| original_filename | 原始文件名，仅作展示 |
| storage_path | 服务生成的本地相对存储路径，不返回给客户端 |
| size_bytes | 实际文件字节数 |
| transcript | 转写文本，可为空 |
| summary_result | MySQL JSON：summary、key_points、todos，可为空 |
| created_at / updated_at | UTC 时间，接口返回带时区的 ISO 8601 时间 |

### tasks：执行状态

| 字段 | 含义 |
| --- | --- |
| id | UUID，任务唯一标识 |
| recording_id | recordings 外键，同时加唯一约束 |
| status | pending / transcribing / summarizing / done / failed |
| attempt | 首轮为 1；手动重试时递增 |
| error_code / error_message | 最近失败原因，不能暴露密钥或内部堆栈 |
| created_at / updated_at | 任务创建与状态更新时间 |
| started_at / finished_at | 当前轮次开始、结束时间，可为空 |

索引：录音创建时间与 ID 组合索引；任务 status 与 created_at 组合索引；recording_id 唯一索引。删除录音时关联任务通过外键级联删除。状态、文本和结果变更在短事务内提交，不在等待转写或 DeepSeek 响应期间持有事务。

## 4. 接口与验收约定

| 接口 | 行为 | 成功状态码 |
| --- | --- | --- |
| POST /v1/recordings | multipart/form-data，字段 file；创建录音与任务；返回 recording_id、task_id、status=pending | 202 |
| GET /v1/tasks/{task_id} | 返回任务 ID、录音 ID、状态、attempt、错误信息与时间 | 200 |
| GET /v1/recordings?page=&page_size= | 返回 items、total、page、page_size，含每条关联任务状态 | 200 |
| GET /v1/recordings/{id} | 返回录音元数据、任务状态、已有 transcript 与 summary_result | 200 |
| POST /v1/tasks/{task_id}/retry | 仅 failed 可重试，返回同一 task_id 和 pending | 202 |
| DELETE /v1/recordings/{id} | 删除这一条录音的文件、录音记录和任务 | 204 |

补充一个供启动检查使用的 GET /health，数据库可访问时返回 200，否则返回 503。

### 上传与查询

- 文件必须存在且非空，扩展名大小写不敏感，仅允许 wav、mp3、m4a、aac。按题目不做音频解码和 MIME 内容鉴定。
- 文档将 50MB 明确解释为 50 × 1024 × 1024 字节；分块读取并按实际字节数校验，不能仅信任请求头。不符合要求时只清理本次上传产生的明确路径文件。
- 使用 UUID 生成磁盘文件名，用户文件名不能决定保存路径。文件保存成功后，在一个事务中创建录音和任务；事务失败时清理本次文件。
- “立即返回”指上传保存和建库完成后返回，不等待 5～15 秒转写或摘要请求；不代表无需等待文件上传。
- page 默认 1，page_size 默认 20、最大 100；非法值返回 400。列表按 created_at、id 倒序稳定排序，空列表返回 200。
- 详情在摘要尚未完成时返回 summary_result=null；可以提前返回已持久化的 transcript。完成后必须包含完整转写与摘要。

### 统一错误

统一响应包含 error.code 和 error.message；输入校验错误也使用该结构。内部异常详情只写日志。

| 场景 | HTTP 状态码 |
| --- | --- |
| 缺少文件、空文件、扩展名不支持、分页或 ID 格式不合法 | 400 |
| 超过 50MB | 413 |
| 合法 ID 对应资源不存在 | 404 |
| 重试非 failed 任务；删除尚在排队或执行的录音 | 409 |
| 请求处理期间数据库或文件操作异常 | 500 |

后台转写失败、DeepSeek 超时或输出不合法，不改变先前上传请求的成功响应。查询接口仍返回 200，但任务状态为 failed，并提供业务错误码，例如 ASR_FAILED、LLM_TIMEOUT、LLM_INVALID_OUTPUT。

## 5. 异步任务、重试与删除的一致性

### 调度与执行

1. 上传或手动重试在数据库事务提交后，使用 asyncio 启动对应后台任务；保留任务引用并在结束时移除引用。不轮询数据库、不建立受并发上限控制的等待队列。
2. 执行开始时以 pending 为条件更新到 transcribing；仅更新成功的执行者继续，避免同一任务被重复启动。数据库会话按任务独立创建。
3. Mock 转写使用异步等待，随机耗时 5～15 秒，约 20% 概率失败；成功返回一段适合摘要的会议文本。该文本不代表真实音频内容，必须在 README 中说明。
4. 转写成功后，将 transcript 与 summarizing 状态在同一事务持久化，然后调用 DeepSeek。
5. 验证摘要后，将结果与 done 状态在同一事务持久化；失败则保存 failed、错误码和结束时间。
6. 每个后台任务独立捕获异常，一个任务失败不影响其他任务。若异常时数据库仍可写，保存 failed；若数据库不可写，记录未能落库的错误，不自动重跑业务流程。数据库恢复后重启应用，将遗留未完成状态标记为 failed，再由用户手动重试。

### DeepSeek 摘要

- 使用真实 API，模型、基础地址、请求超时通过环境变量配置；默认请求总等待预算约 60 秒，并单独设置连接超时。
- 使用 JSON Output，并在提示词中明确 JSON 示例及三项字段。JSON Output 的使用要求见 [DeepSeek 官方文档](https://api-docs.deepseek.com/guides/json_mode/)。
- 仍需解析并严格校验结果：summary 是非空字符串，key_points 和 todos 是字符串数组，没有待办时 todos 可以为空。JSON 可解析不代表字段符合要求。
- 空内容、截断结果、无法解析的 JSON、字段缺失或类型错误，都按失败处理；不能编造默认摘要冒充成功。
- 连接错误、超时、鉴权失败、额度不足、限流和上游错误均转换成可读的任务错误。不得在日志中输出 API Key。
- 首版没有自动重试，避免隐藏额外请求和扣费；失败后走题目要求的手动重试。具体请求字段在实现时依据 [DeepSeek Chat Completions 官方接口](https://api-docs.deepseek.com/api/create-chat-completion/) 核对。

### 手动重试与重复请求

在事务中以“状态仍为 failed”为条件更新到 pending，并递增 attempt、清空旧错误和当前轮次时间。并发或连续重复请求中只有第一条成功，其余返回 409，不产生新任务、不重复排队。

如果此前转写已经成功，则保留 transcript，重试时跳过 Mock，直接继续摘要；转写失败则重新转写。允许 pending → summarizing 作为已有转写的恢复分支，正常首轮仍遵循题目四阶段顺序。手动重试次数不设上限；选做的“最多 3 次自动重试”不在本计划范围内。

### 重启行为

不实现服务重启自动恢复执行。应用启动、接收新请求前，将遗留的 pending、transcribing、summarizing 全部标记为 failed，错误码为 SERVICE_RESTARTED，记录结束时间；保留已有 transcript。done 和 failed 不变，只有客户端显式调用重试接口才重新执行。

该处理用于明确呈现中断结果、避免永久卡住，不领取或继续执行任何遗留任务。关闭时停止进程内任务并释放资源，未完成状态由下一次启动统一处理。

DeepSeek 已返回但数据库尚未提交时若发生崩溃，之后的手动重试可能再次调用 API，因此可能重复计费。上传文件与 MySQL 数据使用持久卷，容器重建后仍保留记录，但不自动继续处理。

### 删除行为

- pending、transcribing、summarizing 返回 409；允许删除 done 或 failed。这样无需引入取消任务协议。
- 删除和重试锁定同一任务行，防止 failed 录音刚开始删除又被重试。
- 在锁定状态后删除该录音唯一对应的磁盘文件，再删除数据库记录并提交；文件已不存在视为已完成文件清理，其他文件错误返回 500 并保留记录。
- 若文件已删除而数据库提交失败，记录可能暂时保留；再次 DELETE 可完成清理。文件系统和数据库无法实现单一原子事务，此限制写入 README。
- 所有文件操作校验路径位于上传目录，只对单个明确文件执行删除。遵守本项目约束：禁止批量删除文件或目录，不使用递归删除命令。

## 6. 分 6 步开发与 GitHub 提交

每一步只编写当前阶段需要的文件，通过对应验收后进行独立 commit，并推送 GitHub；不先生成全部实现后拆分提交，不压缩合并历史，也不伪造提交时间。每步可以因修复问题追加小提交。

第 2 步先验收文件和持久化服务，第 3 步接入进程内后台任务后才开放完整上传流程。

| 步骤 | 具体开发内容 | 阶段验收与提交建议 | 预计时间 |
| --- | --- | --- | --- |
| 1. 环境与骨架 | 核对 GitHub 和环境；建立配置、依赖、FastAPI 入口、健康检查、MySQL 连接、SQL、忽略文件和应用/MySQL Compose 基础配置 | 能启动并连接 MySQL，SQL 创建两张表，无真实密钥进入 Git；提交 chore: initialize FastAPI and MySQL foundation | 1.5 小时 |
| 2. 文件与录音持久化 | 文件扩展名与大小校验、UUID 路径、分块保存、录音和任务事务创建、失败清理；建立错误处理和日志基础 | 验证有效文件、缺文件、空文件、大小边界和数据库失败清理；提交 feat: persist uploaded recordings and pending tasks | 2 小时 |
| 3. 异步转写与查询 | 开放上传接口，增加 asyncio 后台任务、Mock 转写、任务/列表/详情查询，以及重启后统一标记失败 | 上传不等待 Mock；可观察阶段；任务不会重复启动；成功转写暂保留 summarizing，下一步接通摘要；提交 feat: add async transcription and query APIs | 2 小时 |
| 4. DeepSeek 闭环 | 真实 API 客户端、提示词、JSON 与字段校验、超时及上游错误、结果持久化 | 至少一条任务真实到达 done；人工核验超时及输出校验分支；提交 feat: generate validated summaries with DeepSeek | 2 小时 |
| 5. 重试、删除与错误完整性 | 条件重试、attempt、保留转写、重复请求保护、删除行锁与单文件清理；补齐所有错误结构和生命周期日志 | 两次并发重试只执行一次；终态删除同时移除文件与关联数据；活动任务删除返回 409；提交 feat: support safe retry and recording deletion | 2 小时 |
| 6. 本地验收与一键运行 | 完善 Dockerfile/Compose、数据库健康检查、初始化和持久卷；准备 Postman Collection、README、架构图与表说明；人工检查 P0 全流程 | 在干净配置下完成一键启动及上传→查询→结果/失败→手动重试→删除；验证重启只标记失败、不自动续跑；提交 docs: add local setup and API acceptance guide | 1.5 小时 |

六步原估算合计约 11 小时，另预留环境和联调时间；环境安装、账号充值或等待授权可能增加总时间。第 3 步是明确的开发中间态，不作为完整交付；到第 4 步才有端到端成功路径。


后续每一步结束时提供：改动摘要、验收结果、未解决事项、commit 哈希与推送状态，然后继续下一步。遇到需要你提供的配置时，说明具体阻塞项。GitHub 凭据未就绪时可以完成本地工作并保留真实本地提交，但不能宣称已经推送。

## 7. 最小目录与交付组织

预计业务实现控制在约 8 个模块：应用入口、配置、数据库、数据模型、请求/响应结构、路由、后台任务、DeepSeek 客户端。只有文件逻辑明显影响路由可读性时才拆出 storage 模块，不预建多层 repository/service 框架。

最终仓库包含 app/、sql/001_init.sql、README.md、依赖文件、Dockerfile、compose.yaml、.env.example、.gitignore 和 Postman Collection，以及本地运行文档。不创建 tests/ 或自动化测试套件。本方案可作为 docs/ 下的开发设计文档随第 1 步提交。

一键启动约定：首次复制环境变量示例并填写配置，此后执行 docker compose up --build 启动应用和 MySQL，自动等待数据库就绪并在空数据库初始化表。健康检查依赖遵循 [Docker Compose 官方启动顺序说明](https://docs.docker.com/compose/how-tos/startup-order/)。已有数据库的后续结构变更通过新的编号 SQL 显式执行，不假设初始化脚本会重复运行。

README 必须说明：依赖和启动步骤、环境变量、架构流程图、表结构、接口与人工验收流程、Mock 性质、重启后任务失败且需手动重试、重复调用风险、删除限制、单实例限制、本地运行与维护步骤，以及明确不实现的第 1～6 条加分项。

.gitignore 排除实际 .env、私钥、上传文件、运行日志、数据库数据、虚拟环境和测试缓存；.env.example 只包含占位值。上传录音和原始题目 PDF 默认不推送，避免提交无关数据；是否提交题目 PDF 由你决定。

## 8. 需要你提供的信息

### 开发前需要确认

| 信息 | 你需要提供什么 | 缺少时如何处理 |
| --- | --- | --- |
| GitHub 仓库 | SSH 远程地址，例如 git@github.com:用户名/仓库名.git；仓库是否已有内容，目标分支，公开或私有 | 可先本地开发；推送前必须取得准确远程地址并检查历史 |
| Git 身份与权限 | 本机 Git 提交姓名/邮箱是否已配置；本机 SSH 公钥是否已经添加 GitHub，是否有仓库写权限 | 开发时检查；不要向我提供 SSH 私钥 |
| 截止时间 | 收到题目日期、最终提交日期与时间 | 暂按四天、11～14 小时安排 |
| 运行环境 | 是否接受 Docker Compose；本机 Docker Desktop 是否可用；是否必须直接使用现有 MySQL | 默认推荐 Docker Compose 管理 MySQL，避免占用现有数据库 |

### 第 4 步真实联调前需要准备

| 信息 | 配置方式 |
| --- | --- |
| DeepSeek API Key | 你在本机 .env 中填写 DEEPSEEK_API_KEY，告知“已配置”即可；不用发送密钥正文 |
| DeepSeek 模型 | 提供希望使用的模型 ID；不指定则开发时核对官方可用模型与成本后选择适合结构化摘要的模型 |
| API 地址 | 默认官方 https://api.deepseek.com；如使用特殊接入地址需提前说明 |
| 调用条件 | 确保账号额度可用，本机能够访问 API；如有测试预算上限请告知 |

没有真实 Key 时，可以推进其他功能并人工检查请求和输出校验逻辑，但不能将模拟摘要当作真实 DeepSeek 验收完成。

### 只有复用现有 MySQL 时才需要提供

主机、端口、数据库名、数据库版本和应用用户名，以及该用户是否具有建表和数据读写权限。密码直接填本机环境变量，不在聊天或文档中记录。使用专用数据库，不对现有业务库执行清空操作。采用 Compose 新建 MySQL 时，无需事先提供服务器地址，只需要在本地配置专用密码。

### 可选信息与默认值

- 录音样例：可提供一个不含敏感信息的小音频；没有样例时后续创建测试音频。Mock 不需要真实语音识别服务。
- 摘要语言：默认中文。
- 是否保留每次重试的数据库历史：默认只保留当前任务，执行轮次和日志满足追踪需要；若明确需要历史，再扩展模型。
- 删除处理中任务：默认返回 409；如题目评审另有要求，再加入取消处理。
- 每步是否暂停讲解：默认每步验收并提交后简要汇报；如你希望逐步阅读确认，可以每轮只实现一步。

## 9. 最终验收清单

1. 准备 .env 后，通过一条 Compose 命令启动应用与 MySQL；数据库和上传目录重启后仍保留。
2. 四种扩展名可上传；文件缺失、为空、超限、类型不符均返回统一错误；50MB 边界有验证。
3. 上传响应包含 recording_id、task_id、pending，且不等待处理完成。
4. Mock 符合 5～15 秒和约 20% 失败的实现要求；通过人工运行、日志与实现检查核对，不以少量运行的失败比例判定正确性，不为此增加自动化测试套件。
5. 正常任务完成所有阶段，至少有一次真实 DeepSeek 成功联调，结果严格满足摘要结构。
6. 超时、非 JSON、空结果、字段错误和上游失败能让任务进入 failed，其他任务继续执行。
7. 列表分页倒序正确；详情、任务阶段和错误信息可查询；不存在资源返回 404。
8. 只有 failed 可以重试；并发重复请求仅有一个成功，复用 task_id，attempt 正确递增。
9. 删除终态录音移除单个文件及关联数据；活动任务返回 409；重复删除返回 404。
10. 在任务未完成时重启应用，遗留 pending、transcribing、summarizing 均变为 failed，错误码为 SERVICE_RESTARTED；不会自动续跑，手动重试后可重新执行。
11. 日志包含 recording_id、task_id、attempt、阶段、耗时和错误码，可还原执行过程，不包含密钥。
12. 使用 Postman 和人工联调验收成功、失败、重复重试与重启行为；对于不易由真实上游触发的非法 JSON 等分支，结合实现检查记录验收依据，不宣称未实际完成的运行验证。不开发展示加分用的单元测试或集成测试套件。
13. 仓库保留六个阶段的独立提交，提供可访问仓库链接；私有仓库需要给评审访问权限。README、SQL、一键启动和 Postman 文件齐全。
14. 能解释：异步为何不阻塞请求、事务如何防止重复执行、为何仅支持单实例、模型输出如何校验、重启为何可能重复调用，以及文件删除与数据库事务的边界。

## 10. 最终交付

项目以已完成的前六步结项，保留本地 Docker Compose、MySQL 持久卷、录音持久卷、全部业务接口和阶段提交记录。运行说明见 [本地运行文档](docs/run-local.md)，人工验收见 [验收指南](docs/local-acceptance.md)。
